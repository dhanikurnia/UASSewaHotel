<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="polinema.ac.id.uassewahotel">

    <application
        android:allowBackup="true"
        android:background="@color/colorblack"
        android:icon="@drawable/hotelo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hotelo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.JogjaActivity" />
        <activity android:name=".activities.SemarangActivity" />
        <activity android:name=".activities.BatuActivity" />
        <activity android:name=".activities.JakartaActivity" />
        <activity android:name=".activities.BaliActivity" />
        <activity android:name=".activities.MalangActivity" />
        <activity android:name=".activities.AboutActivity" />
        <activity android:name=".activities.UangActivity" />
        <activity android:name=".activities.pembayaranActivity" />
        <activity android:name=".activities.RegistrasiActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>